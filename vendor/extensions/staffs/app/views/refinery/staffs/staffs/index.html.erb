<% content_for :body do %>
  <ul id="staffs">
    <% @staffs.each do |staff| %>
      <li>
        <img src="<%= staff.image.url %>" />
        <h2><%= staff.name %></h2>
        <p><%= staff.title %></p>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
